# CPLayout Agent Source Ledger

This ledger records sources used to define the agent, hook, and skill surfaces. Use primary or official sources for claims that affect architecture, platform behavior, database behavior, Google Earth/KML evidence, or real-world irrigation design.

| Source | Verified use | Boundary |
| --- | --- | --- |
| OpenAI Codex AGENTS.md docs, checked 2026-06-03: `https://developers.openai.com/codex/guides/agents-md` | Repository instructions are durable guidance, can be scoped by directory, and should be re-read for non-trivial CPLayout work. | `AGENTS.md` guidance does not itself prove product runtime behavior. |
| OpenAI Codex subagents docs, checked 2026-06-03: `https://developers.openai.com/codex/subagents` | Project-scoped custom agents live under `.codex/agents/`; standalone custom agents require `name`, `description`, and `developer_instructions`; subagents are useful for independent exploration, tests, and review but consume additional model/tool work. | Custom agent format may evolve; parse locally, keep agents narrow, and avoid overlapping write scopes. |
| OpenAI Codex hooks docs, checked 2026-06-03: `https://developers.openai.com/codex/hooks#userpromptsubmit` and `https://developers.openai.com/codex/hooks#stop` | `UserPromptSubmit` can add `hookSpecificOutput.additionalContext`; `PreToolUse` can add advisory context or deny supported tool calls; `Stop` receives `last_assistant_message` and `stop_hook_active`, expects JSON on stdout, and can continue with `decision: "block"` plus `reason`. | Project-local hooks depend on trusted config and hook trust. Stop continuation is script-tested here, not endpoint-proven. |
| OpenAI Codex managed hooks docs, checked 2026-06-03: `https://developers.openai.com/codex/hooks#managed-hooks-from-requirementstoml` | `requirements.toml` can define managed hooks, pin `[features].hooks = true`, and set `allow_managed_hooks_only = true` to skip user, project, session, and plugin hooks while loading managed hooks. | Codex does not distribute scripts from `managed_dir`; endpoint management and restart verification remain required. |
| OpenAI Codex hook trust docs, checked 2026-06-03: `https://developers.openai.com/codex/hooks#review-and-trust-hooks` | Non-managed command hooks require review/trust by hash; managed hooks are trusted by policy and cannot be disabled from the user hook browser. | This does not prove the CPLayout hooks loaded in a live managed session. |
| OpenAI Codex requirements reference, checked 2026-06-03: `https://developers.openai.com/codex/config-reference#requirementstoml` | `requirements.toml` supports `allow_managed_hooks_only`, `[features]`, and managed `[hooks]` with absolute managed directories and event tables. | Requirements behavior must still be verified on the deployed Codex surface and endpoint. |
| OpenAI Codex config reference, checked 2026-06-03: `https://developers.openai.com/codex/config-reference#configtoml` | Project `.codex/config.toml` can define `[features].hooks`, `[agents]`, and `agents.<name>.config_file`; `model_reasoning_effort` exists as a supported config key but CPLayout now selects reasoning per task instead of pinning it project-wide. | User-level provider/auth/telemetry keys belong outside project config; hook advice does not mutate an already-running session's model settings. |
| Skill creator validator: `/home/cyber/.codex/skills/.system/skill-creator/scripts/quick_validate.py` | Validates `SKILL.md` frontmatter names, descriptions, and allowed fields. | Local tool availability is machine-specific. |
| Google KML Reference: `https://developers.google.com/kml/documentation/kmlreference` | KML is a geographic display format with shared styles; style metadata is presentation evidence for CPLayout, not canonical projected `XY` geometry. | Google Earth rendering still requires visual proof. |
| OpenCV Hough Circle Transform: `https://docs.opencv.org/4.x/d4/d70/tutorial_hough_circle.html` | Baseline local companion detector for circular crop-ring and overlay-circle candidates; `HoughCircles` finds circles in grayscale images and exposes tunable radius/threshold parameters. | Image-space circle evidence is not projected `XY` truth without calibration, fixture metrics, and review. |
| OpenCV Canny Edge Detection: `https://docs.opencv.org/4.x/da/d22/tutorial_py_canny.html` | Baseline edge cue for crop rings, radial/tower tracks, roads, fencelines, and structures in local screenshots. | Edge response requires threshold sweeps and false-positive audits before it can support a recommendation. |
| scikit-learn cross-validation: `https://scikit-learn.org/stable/modules/cross_validation.html` | Source for train/validation/test hygiene and avoiding evaluation on training examples in local ML experiments. | Small agricultural fixture sets still need project/field-level split isolation and leakage checks. |
| MLflow Tracking: `https://mlflow.org/docs/latest/ml/tracking/` | Local experiment record candidate for parameters, metrics, and artifacts under ignored local outputs. | No remote MLflow server, upload, telemetry, or account requirement by default. |
| DVC add command: `https://doc.dvc.org/command-reference/add` | Source for lightweight dataset/model pointer metadata via `.dvc` files instead of committing large imagery. | Remote DVC storage is not assumed and must not be required for the local-first loop. |
| ONNX Runtime React Native: `https://onnxruntime.ai/docs/get-started/with-javascript/react-native.html` | Candidate future React Native inference runtime if a development build/device checklist is satisfied. | Not installed or production-approved for CPLayout pivot locating. |
| ONNX Runtime mobile deployment: `https://onnxruntime.ai/docs/tutorials/mobile/` | Source for mobile inference deployment concepts and proof targets. | Device performance, memory, power, network isolation, and parity remain unverified. |
| Expo development builds: `https://docs.expo.dev/develop/development-builds/introduction/` | Expo documents that native-library behavior requires a development build rather than assuming Expo Go can run new native code. | Native ML claims require Android/iOS evidence, not docs alone. |
| Expo SQLite docs: `https://docs.expo.dev/versions/latest/sdk/sqlite/` | Expo SQLite provides SQLite access across supported platforms; web setup requires WASM support and COOP/COEP headers, and web support is alpha. | CPLayout web SQLite remains research-gated until deployment headers and browser behavior are verified. |
| Expo SQLite query safety docs, checked 2026-06-02: `https://docs.expo.dev/versions/latest/sdk/sqlite/` | `execAsync` is documented as not escaping parameters; dynamic values should use prepared, bound, or tagged-template APIs. | CPLayout migration/static PRAGMA SQL may stay static; dynamic project/catalog values must stay bound. |
| Expo FileSystem docs, checked 2026-06-03: `https://docs.expo.dev/versions/latest/sdk/filesystem/` | Expo FileSystem exposes `File`, `Directory`, and `Paths.document` APIs for app document storage; CPLayout uses that storage for imported native map package files. | FileSystem API shape does not prove Android MapLibre can render a given local tile URI until a native screenshot report passes. |
| MapLibre React Native RasterSource docs, checked 2026-06-03: `https://maplibre.org/maplibre-react-native/docs/components/sources/raster-source/` | Raster sources use TileJSON URLs or tile URL templates and attribution metadata. CPLayout's native proof harness uses a generated localhost tile URL template through this supported source shape. | This does not prove raw MBTiles/PMTiles native archive rendering or runtime behavior without an Android/iOS screenshot report. |
| MapLibre React Native VectorSource docs, checked 2026-06-03: `https://maplibre.org/maplibre-react-native/docs/components/sources/vector-source/` | Vector sources use TileJSON URLs or vector tile URL templates, and query loaded vector tile features by source layer. | This does not provide a local raw PMTiles/MBTiles native protocol by itself. |
| MapLibre Style Spec sources/layers docs, checked 2026-06-03: `https://maplibre.org/maplibre-style-spec/sources/` and `https://maplibre.org/maplibre-style-spec/layers/` | Vector source descriptors can use URL/template metadata; vector style layers require the source-layer contract for layers inside an MVT source. | This is style/source metadata evidence, not native runtime proof. |
| USGS EROS NAIP docs, checked 2026-06-03: `https://www.usgs.gov/centers/eros/science/usgs-eros-archive-aerial-photography-national-agriculture-imagery-program-naip` | NAIP is the primary free/no-cost offline U.S. farm-scale aerial source lane for CPLayout package preprocessing. | Source data still has to be downloaded/preprocessed outside the app and attributed in package metadata; CPLayout does not run GDAL in React Native. |
| USGS TNM ImageryOnly service metadata, checked 2026-06-03: `https://basemap.nationalmap.gov/arcgis/rest/services/USGSImageryOnly/MapServer?f=pjson` | The optional connected preview uses the USGS cached orthoimagery tile service and visible attribution. | Live service use is interactive preview only; no bulk caching, offline package generation from public tile endpoints, or project export of live URLs. |
| GDAL raster tile docs, checked 2026-06-03: `https://gdal.org/en/stable/programs/gdal_raster_tile.html` | Offline preprocessing may use GDAL outside the app to generate separate raster tile files from NAIP raster datasets. | This is a companion/offline preprocessing claim, not React Native runtime GIS capability. |
| Protomaps PMTiles MapLibre docs, checked 2026-06-02: `https://docs.protomaps.com/pmtiles/maplibre` | Browser MapLibre integration uses the PMTiles JavaScript protocol with `maplibregl.addProtocol`, and React apps should register it once per application lifecycle. | This is browser/Web MapLibre evidence, not native MapLibre React Native proof. |
| SQLite about/docs: `https://www.sqlite.org/about.html` and `https://www.sqlite.org/docs.html` | SQLite is embedded, serverless, public domain, transactional, and single-file oriented, aligning with offline-first local project storage. | Runtime behavior must still be validated on target platforms. |
| USDA NRCS NEH Part 623 Chapter 11 Sprinkler Irrigation: `https://www.wcc.nrcs.usda.gov/ftpref/wntsc/waterMgt/irrigation/NEH15/ch11.pdf` | Seed primary engineering reference for center-pivot design research and terminology. | Do not treat this ledger as engineering certification or complete design coverage. |

## Continuous Improvement Loop Records

| Record | Verified use | Boundary |
| --- | --- | --- |
| `SRC-WHOLE-LOOP-VERIFY`: `tools/verify_whole_codebase_improvement_loop.ts` | Verifies the whole-codebase 100-row ledger for row continuity, guardrail phrases, valid decision states, weighted vote cells, and evidence requirements for rows marked `Pass`. | It does not prove planned future rows executed; it only enforces structure and passed-row evidence fields. |
| `SRC-WHOLE-LOOP-LEDGER`: `docs/whole-codebase-improvement-loop-2026-06-01.md` | Coordinates the 10-batch whole-codebase loop across UI, storage, maps, overlays, ML/CV, native gates, evidence, and final validation. | Rows marked `Planned` are roadmap rows, not completed implementation. |
| `SRC-LOOP-CLAIM-CLASSES` | Separates browser proof, storage proof, synthetic ML/CV proof, real-world fixture proof, native/device proof, Google Earth proof, and documentation proof. | Success in one claim class must not be reported as success in another. |
| `SRC-CHECKPOINT-POLICY` | Current dirty-tree work must be investigated, validated, committed, and remote-checkpointed before new high-risk loop batches proceed. | Checkpointing preserves pre-existing work; it is not a production readiness claim. |
| `SRC-BATCH1-SOURCE-REFRESH-2026-06-02` | Rechecked Codex subagents, Expo SQLite, MapLibre React Native RasterSource/VectorSource, Protomaps PMTiles MapLibre, and OpenCV Hough/Canny sources for whole-codebase loop rows 002-010. | This is a source snapshot for batch 1 only; future architecture edits must refresh sources again when claims are time-sensitive. |
| `SRC-BATCH1-CHECKPOINT-0e8173f` | Dirty work from the prior checkpoint was validated, committed as `0e8173f136fe5ea50a6fafd8066a43e527a8fc3f`, and tracked by `origin/codex/cplayout-agent-specialists`. | Commit/push proof is version-control evidence, not runtime production proof. |
| `SRC-BATCH1-EVIDENCE-TAXONOMY` | Batch 1 keeps proof classes separate: browser, storage, synthetic ML/CV, real-world fixture, native/device, Google Earth, and documentation. | Passing one proof class does not imply another class passed. |
| `SRC-WHOLE-LOOP-BLOCKED-ROW-INVENTORY` | Remaining blockers include native/device proof, real-world ML/CV fixtures, project-CRS calibration, raw PMTiles/MBTiles native rendering, per-artifact Google Earth render proof for new exports, ZIP safety hardening, and large-scale stress. | These blockers require future implementation or external evidence before they can become pass claims. |
| `SRC-WHOLE-LOOP-ROWS-011-100-AUTOMATION` | Rows 011-100 were batch-classified by the local automation pass as `Pass` or `Blocked` based on available evidence. | This is risk inventory, not proof of 100 individual material codebase improvement iterations. |
| `SRC-PIVOT-CANDIDATES-COMMAND` | `tools/local-ml-companion` now exposes `detect-pivot-candidates`, which runs local OpenCV Hough-circle pivot-center sweeps and emits `cplayout-pivot-candidates-v1` review evidence plus metadata-only recommendations. | The command does not emit `proposedGeometry.pivotCenter` without project-CRS calibration and does not prove native/mobile ML behavior. |
| `SRC-ARCHIVE-ADJACENT-REVIEW-2026-06-02` | `packages/project-store/src/projectArchive.ts` now exposes `importProjectArchiveZipWithAdjacentData` for project, adjacent review data, and manifest; the compatibility wrapper still returns `PivotProject`. `ProjectFilesPanel` exports current adjacent review data and restores it after browser ZIP import. | Browser archive behavior is proven through project-store and Playwright tests. Android canonical ZIP share/picker behavior is separately proven by `SRC-ANDROID-NATIVE-RUNTIME-2026-06-03`; iOS and large-scale stress remain separate gates. |
| `SRC-REAL-PIVOT-FIXTURE-MANIFEST-2026-06-02` | `tools/local-ml-companion` accepts `cplayout-real-pivot-fixtures-v1` manifests through `build-evidence-packet --real-pivot-fixtures`, hashes local artifacts, rejects hash mismatches, and emits projected-XY pivot recommendations only for calibrated operator-approved truth. | No real-world fixture set was supplied in this pass; the manifest path is a gate, not automatic projected-XY pivot-locating proof. |
| `SRC-ROADMAP-COMPLETION-AUTOMATION-2026-06-02` | `tools/roadmapCompletion.ts` and `npm run verify:roadmap` run local validation, browser proof, native/device detection, Google Earth manifest validation, real-pivot fixture detection, typed native MapLibre report validation, and native adjacent-review-data persistence classification into ignored completion reports. | This reduces operator babysitting by automating eligible decisions; it still cannot fabricate missing devices, real fixtures, native screenshots, Google Earth render captures for new artifacts, or qualified field-review evidence. |
| `SRC-NATIVE-ADJACENT-SQLITE-2026-06-03` | `ProjectRepository` now exposes optional adjacent review-data load/save methods; `createSqliteProjectRepository` persists parsed evidence, model recommendations, and layout decisions in the v4 SQLite tables. `npm run verify:roadmap:fast` marks the code/API gate pass. | Android runtime persistence for the canonical project package is now covered by `SRC-ANDROID-NATIVE-RUNTIME-2026-06-03`; adjacent review-data device stress and iOS remain future gates. |
| `SRC-REAL-PIVOT-FIXTURE-GENERATOR-2026-06-03` | `tools/generateRealPivotFixtureManifest.ts` and `npm run real-pivot-fixture:generate` create `fixtures/real-pivot/manifest.json` from the public Adams County project reference and local Google Earth proof artifacts with hashes. The 2026-06-03 fast roadmap run produced a projected-XY pivot-center recommendation from that manifest. | This proves one curated fixture path; it is not broad automatic pivot-locating model quality and still requires Review Apply XY for mutation. |
| `SRC-NATIVE-MAPLIBRE-PROOF-HARNESS-2026-06-03` | The first native MapLibre proof harness used an env-gated raster TileJSON/template panel and generated PNG tiles. A prior local run recorded Android generated TileJSON/template evidence, but its ignored `latest.json` artifact is mutable and is no longer a stable checked-in proof reference. | Treat the raster run as historical context unless a stable report path/hash is promoted. Raw PMTiles/MBTiles native rendering and iOS behavior remain unproved. After the vector-harness change, a new device report is required before claiming Android vector `VectorSource` rendering. |
| `SRC-NATIVE-MAPLIBRE-VECTOR-HARNESS-2026-06-03` | `packages/map-adapters/src/NativeMapLibreTileProofSurface.native.tsx` now renders the proof fixture through MapLibre RN `VectorSource` and vector `Layer` entries with source layers for roads, borders, labels, and places. `tools/nativeMapLibreProof.ts` now serves generated `.pbf` vector tiles and writes vector source metadata for roadmap validation. | This is implementation and report-schema evidence only until `npm run verify:native-maplibre` is rerun on a native device/emulator and produces a passing screenshot report. Raw PMTiles/MBTiles archive rendering remains unproved. |
| `SRC-WEB-TABLET-CONSOLE-LAYOUT-2026-06-03` | The map route now uses a fixed tablet console with collapsible project and inspector drawers, safe-area bottom guttering, flex-filled SVG/MapLibre surfaces, and Playwright tablet portrait/landscape checks for no page scroll, drawer handles, bottom HUD visibility, and touch target floors. | This is browser export evidence. Android SM-P613 native safe-area/system-navigation layout still needs the checklist in `docs/android-native-verification.md`. |
| `SRC-ANDROID-DEVICE-TRANSPORT-2026-06-03` | Windows ADB, WSL ADB, `usbipd`, PnP, ADB mDNS, and local TCP 5555 probes initially found no live Samsung tablet; after cable replacement and USBIPD/ADB reattachment, `adb devices -l` exposed Samsung SM-P613 serial `R52W20BK7XH` in `device` state. | This records the host transport dependency. Future runs must still re-run `npm run check:android-tools` because WSL/USB reconnects can invalidate ADB state. |
| `SRC-ANDROID-NATIVE-RUNTIME-2026-06-03` | `npm run verify:android-native -- --report reports/android-native-verification/android-native-verification-20260603T034817Z.json` passed on Samsung SM-P613 (`R52W20BK7XH`, Android 14/API 34). The report covers Expo SQLite schema v8, migrations 1-8, save/relaunch/list/load/delete, native share-sheet ZIP export, Android DocumentsUI ZIP import, and manifest/project id/CRS agreement for `sample-burgundy-quarter-section`. | This proves Android native SQLite and canonical ZIP share/picker behavior for the tested development client. It does not prove iOS, raw PMTiles/MBTiles native rendering, on-device ML, live GNSS, or broad real-world automatic pivot locating. |
| `SRC-ANDROID-FREE-AERIAL-IMAGERY-2026-06-03` | Core settings now separate local `aerialImagery` preferences from session-only `onlineImagery`; map package manifests carry optional NAIP-style `imageryProvenance`; project-store v9 adds `map_packages.imagery_provenance_json`; Files exposes native `cplayout-map-package-v1.zip` import; project metadata keeps logical `app://map-packages/<id>/...` URLs while runtime state keeps rewritten app-readable URLs for MapLibre; web/native MapLibre surfaces resolve generated raster TileJSON/templates with attribution. | This is code, schema, and web-renderer integration evidence. Android imported local raster package rendering still needs a completed development-client screenshot report with tile request/pixel evidence on the target device. |

## Development Plan Records

| Record | Verified use | Boundary |
| --- | --- | --- |
| `SRC-FOCUSED-PLAN-2026-06-02`: `docs/cplayout-focused-development-plan.md` | Consolidates verified active plans, completed browser evidence, current local companion status, Google Earth proof status, and unresolved native/ML/archive blockers after a subagent-assisted workspace review. | This is a plan-of-record and status synthesis; it does not add product features or prove blocked runtime claims. |

## Update Rules

- Add sources when a specialist makes a package, platform, database, imagery, or engineering claim.
- Record the claim, source URL or local path, date checked, and what remains unverified.
- Keep real-world design assumptions separate from confirmed project facts.
- Do not use operator labels, imagery evidence, or KML/KMZ styling as automatic geometry mutation authority.
