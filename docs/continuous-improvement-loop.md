# CPLayout Browser Mapping Continuous Improvement Loop

Loop id: `browser-mapping-2026-05-31`

Scope: browser mapping, dashboard, review, files, settings, survey, map adapters, and supporting proof tooling. Raw screenshots, traces, and logs stay under ignored `reports/continuous-improvement/`; only curated milestone evidence is checked in.

Non-goals: no paid APIs, no hidden keys, no Google Maps/paid Mapbox/Esri services, no native MapLibre or raw PMTiles/MBTiles runtime proof claim, no native SQLite production claim, and no schema change that promotes WGS84 or imagery/CV evidence into canonical project geometry. Project geometry remains projected/local `XY`.

Validation cadence:

- Every code/UI iteration: `npm run validate`, `git diff --check`, `npm audit`, plus focused tests for touched packages.
- Visible browser iterations: `npm run proof:web`, which runs `npm run export:web` and the Playwright route sweep at desktop, 768 px, and 390 px widths.
- Every 10th iteration: add `npm run validate:skills`, `npm run audit:moderate`, curated milestone evidence, and a browser proof summary.

## Ledger

| Iteration | Goal | Research Source | Changed Modules | Validation | Artifact Hashes | Commit SHA | Decision | Next Target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 001 | Add dev-only static web proof route sweep for launcher, workspace, map, survey, review, files, and settings. | Repo preflight; existing Expo `export:web`; Playwright config/test discovery; CPLayout AGENTS constraints. | `package.json`; `package-lock.json`; `.gitignore`; `playwright.config.ts`; `tools/serveStaticWeb.ts`; `tests/web/browser-workflow.spec.ts`; `apps/mobile/App.tsx`; `apps/mobile/src/components/SettingsPanel.tsx`; `apps/mobile/src/components/ExpertReviewPanel.tsx` | `npm run typecheck`; `npx playwright test --config playwright.config.ts --list`; `git diff --check`; `npm run test:web:e2e`; `npm run proof:web`; baseline `npm audit` found 0 vulnerabilities. | `package.json` 97b85e86; `playwright.config.ts` d083ddcf; `tools/serveStaticWeb.ts` 48b94732; `tests/web/browser-workflow.spec.ts` 34f76305. | `e1ddd44` | Pass after fixing a strict selector collision between status text and the workflow mode button. | Add map workbench instruction and status clarity without mutating geometry contracts. |
| 002 | Preserve optional WGS84 browser-click metadata when placing pivot or infrastructure from MapLibre, without making it canonical geometry. | Existing `browserMapClickToProjectedIntent` return shape and reducer optional `wgs84` fields. | `packages/map-adapters/src/types.ts`; `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `apps/mobile/App.tsx`; loop ledger. | `npm run validate`; `npm test -w @cplayout/map-adapters`; `git diff --check`; `npm audit`. | `types.ts` 3a0b1244; `BrowserMapSurface.web.tsx` 70580471; `App.tsx` 46d2a6f0. | `9ed4e9b` | Pass; optional WGS84 remains callback metadata and reducer input only. | Add map workbench instruction and status clarity. |
| 003 | Make browser-map draft clearing and workflow-mode changes report local status without mutating committed geometry. | Current `BrowserMapSurface.web.tsx` draft state separation and AGENTS pan/zoom/draft safety constraint. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; loop ledger. | `npm run validate`; `npm test -w @cplayout/map-adapters`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 7d6a4732; ledger 24025915. | `cd9118f` | Pass; draft clear/status changes remain local component state. | Add focused draft/cancel regression coverage. |
| 004 | Add reducer coverage proving draft cancel clears only draft state and preserves the map viewport. | Existing `reduceDrawingMapState` draft and viewport semantics. | `packages/geometry/src/mapInteraction.test.ts`; loop ledger. | `npm run validate`; `npm test -w @cplayout/geometry`; `git diff --check`; `npm audit`. | `mapInteraction.test.ts` c8916159; ledger 23495944. | `b16fc58` | Pass; clearing draft increments draft revision while preserving viewport. | Improve Review Layout no-mutation browser proof. |
| 005 | Interface checkpoint: prove Review Layout map clicks remain read-only and do not dirty the browser project. | Read-only interface review against Playwright route proof and existing Review Layout status surface. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` d5452d79; ledger 40fa781e. | `f6ba27e` | Pass; Review Layout click emits read-only status and leaves Saved state visible. | Tighten mobile HUD fit. |
| 006 | Tighten compact web-map HUD spacing so Review Layout and attribution badges do not compete with the bottom action HUD. | Current mobile Playwright viewport target and `BrowserMapSurface.web.tsx` compact styles. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 82a7d073; ledger 1a12f6c8. | `dd82b44` | Pass; static web proof passed at desktop, 768 px, and 390 px. | Add a mobile screenshot assertion for compact HUD evidence. |
| 007 | Add stable map HUD test IDs and a Playwright no-overlap check for status and attribution HUDs. | Playwright route proof artifacts and compact HUD risk from iteration 006. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 3df5b53b; `browser-workflow.spec.ts` 5a9be95a; ledger 65e87c85. | `1cfa97d` | Pass after moving status HUD higher; overlap gate passes at desktop, 768 px, and 390 px. | Improve draft commit affordance language. |
| 008 | Split stable MapLibre viewport frame from mutable overlay GeoJSON so draft edits do not force camera remount/reset. | Interface specialist checkpoint; current MapLibre construction dependencies in `BrowserMapSurface.web.tsx`. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` e1a3c873; ledger 4c3bd610. | `e27f5bc` | Pass; overlay source updates can change without reconstructing the map instance. | Add browser proof for draft state across tool changes. |
| 009 | Add stable browser map tool/action test IDs and prove tool switching clears draft vertices without dirtying the project. | Interface specialist checkpoint; existing browser map status HUD and Playwright route proof. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 3a7f0ae6; `browser-workflow.spec.ts` 0c140f7d; ledger 8d6d3c47. | `b2c05ac` | Pass; Boundary draft click reaches 1 draft point, Pan clears to 0, and project remains Saved. | Run iteration 010 milestone proof and curate evidence. |
| 010 | Curate first browser mapping milestone evidence and run the 10th-iteration proof gates. | Iterations 001-009 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; interface and KB curator checkpoints. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-010/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-010/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `README.md` 7a6958d9; `SHA256SUMS.txt` d5b760da. | `f05f541` | Pass; browser-only milestone evidence is curated without checking in raw screenshots. | Browser feature selection parity. |
| 011 | Wire BrowserMapSurface map-feature clicks to the existing selected map feature side-panel editor. | Interface specialist checkpoint; SVG selection behavior; `projectLayoutToWgs84FeatureCollection` map feature IDs. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 17d32c76; ledger 9081a8a9. | `6cafee6` | Pass; rendered map-feature clicks in Pan or Review Layout select side-panel metadata without geometry mutation. | Browser feature selection proof. |
| 012 | Add Playwright proof that public proof map-feature selection opens the side-panel editor and keeps the project saved. | Public proof project map features; BrowserMapSurface selection callback from iteration 011. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` c45f58dd; ledger 34966842. | `e3b8f89` | Pass; public proof power-line selection populates the side-panel editor value and stays Saved. | Utility point-vs-line save clarity. |
| 013 | Clarify Utility mode save behavior for point features versus line drafts and cover it in browser proof. | Interface specialist checkpoint; existing Utility tool and `Save Feature` enablement behavior. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` ce8a936f; `browser-workflow.spec.ts` 0899b2bd; ledger dc1e1f00. | `ae27865` | Pass; Utility mode reports line and point save behavior before any project mutation. | Offline/no-external-network proof lane. |
| 014 | Add strict offline browser-map proof with imagery off and external requests blocked. | Interface specialist checkpoint; SettingsPanel imagery Off control; Playwright route interception. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` accfbf9e; `browser-workflow.spec.ts` bcfda342. | `5cad1bf` | Pass; imagery Off is reachable as a button and the browser map still drafts projected `XY` with all external requests blocked. | Settings imagery guardrail wording. |
| 015 | Clarify Settings imagery guardrails for offline overlay, project-export exclusions, and projected/local `XY` geometry. | Current `SettingsPanel` imagery copy and AGENTS geometry/export boundaries. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` e412d25d; `browser-workflow.spec.ts` 326df66b. | `7aa3eb1` | Pass; Settings now says live imagery is reference-only and the Off path keeps projected/local `XY` export boundaries visible. | Settings custom source no-key clarity. |
| 016 | Make custom open imagery guidance explicit about no-key templates, paid-host exclusions, and blocked bulk caching. | Existing custom imagery validation and Settings custom-source form. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` 72c1ad1a; `browser-workflow.spec.ts` 3a712f59. | `7c1079c` | Pass; custom-source guidance is visible and the form remains blocked until a valid no-key open source is supplied. | Browser map edit-mode status for saved geometry. |
| 017 | Preserve explicit projected-geometry commit status after draft vertices clear in the browser map HUD. | Current `commitDraft`/`saveMapFeatureLine` status flow in `BrowserMapSurface.web.tsx`. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` ccb1c79c; `browser-workflow.spec.ts` 2141e887. | `1d528f7` | Pass; committing a three-vertex boundary clears draft points, reports the projected `XY` commit, and marks the project dirty. | Browser utility line save status proof. |
| 018 | Prove utility line saves keep projected `XY` feature status after draft vertices clear. | Browser map Utility tool and `saveMapFeatureLine` status from iteration 017. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 0ee39092. | `47ea954` | Pass; saving a two-vertex utility line clears draft points, reports projected `XY` feature status, and marks the project dirty. | Browser point feature status proof. |
| 019 | Make Utility point saves report projected `XY` map-feature status and prove the dirty-state transition. | Browser map point-feature click intent and Utility point option. | `packages/map-adapters/src/BrowserMapSurface.web.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `BrowserMapSurface.web.tsx` 65aca6c7; `browser-workflow.spec.ts` cceab8b7. | `d12a3d2` | Pass; saving a Utility point reports projected `XY` map-feature status without draft points and marks the project dirty. | Iteration 020 milestone proof and evidence. |
| 020 | Run second milestone gates and curate browser-mapping evidence for iterations 011-019. | Iterations 011-019 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-020/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-020/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `README.md` 4b37cd09; `SHA256SUMS.txt` 142ee130. | `b718fa2` | Pass; milestone proof confirms 21 browser checks and curated hashes without checking in raw screenshots. | Dashboard and walkthrough state guidance. |
| 021 | Make dashboard next-step guidance distinguish imagery Off from live-source attribution confirmation. | Current `recommendedWorkflowStep` logic and Settings imagery Off workflow. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 3c1cf41e; `browser-workflow.spec.ts` f544d831. | `4cb29d3` | Pass; dashboard now points imagery-Off users toward offline overlay or approved no-key imagery instead of live-source confirmation. | Dashboard export-readiness guidance proof. |
| 022 | Add dashboard card anchors and prove export readiness reflects unsaved browser geometry edits. | Dashboard card state and dirty project behavior after browser boundary commit. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 3fcce211; `browser-workflow.spec.ts` 352f8b34. | `bb1b648` | Pass; the export dashboard card is test-addressable and flips to Save before export after browser geometry edits. | Dashboard walkthrough local-only proof. |
| 023 | Add walkthrough module anchors and prove walkthrough progress stays local/export-ready. | Dashboard walkthrough local-only detail and project dirty-state boundary. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` b63166ab; `browser-workflow.spec.ts` 9a7f7673. | `e080a15` | Pass; walkthrough progress updates to 1/7 while the project save state and export card remain ready. | Dashboard warning guidance proof. |
| 024 | Add a Review Warnings dashboard anchor and prove sample warning guidance is visible. | Current `editorWarningRows` output for the sample project and dashboard warning panel. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 09781391; `browser-workflow.spec.ts` 935a8c75. | `fcabb8a` | Pass; Review Warnings has a stable anchor and surfaces obstacle/exclusion plus conflict guidance for the sample project. | Dashboard recent-project empty state proof. |
| 025 | Prove the dashboard recent-project empty state keeps project-start actions visible. | Interface checkpoint cadence; `dashboard-recent-projects` panel anchor from iteration 024. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`; interface specialist checkpoint. | `browser-workflow.spec.ts` cd268368. | `3fe7792` | Pass; empty recent-project state keeps Create New, Open Sample, Real Proof, and Improved Review visible. | Saved recent-project workflow proof. |
| 026 | Make recent-project rows accessible and prove a saved browser project can be reopened from the dashboard. | Interface specialist checkpoint; dashboard recent-project row behavior; web local project repository. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 8d72b860; `browser-workflow.spec.ts` 7f3057f1. | `1ba81a1` | Pass; saving the sample creates an accessible recent-project row that reopens the saved browser project. | Walkthrough project-scope behavior. |
| 027 | Scope walkthrough progress to the active project while keeping it browser-local and export-excluded. | Interface specialist checkpoint; walkthrough localStorage key and dashboard progress card. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 62ebf063; `browser-workflow.spec.ts` 7a9bb9a5. | `013ce24` | Pass; walkthrough progress is stored per project and does not bleed from Sample into Real Proof. | Dashboard next-step priority with dirty/offline state. |
| 028 | Make unsaved projected-geometry edits outrank imagery-Off guidance in the dashboard next step. | Interface specialist checkpoint; `recommendedWorkflowStep` priority and combined imagery-off/dirty geometry scenario. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 7c56cb17; `browser-workflow.spec.ts` f6e6fb98. | `26fb021` | Pass; dirty projected-geometry edits now surface save/export guidance even when live imagery is Off. | Actionable Review Warnings navigation. |
| 029 | Add Review Warnings actions and prove opening Review does not mutate project geometry. | Interface specialist checkpoint; dashboard warning panel to review workflow. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` a2f7a7e9; `browser-workflow.spec.ts` 6ae4bc79. | `10ca260` | Pass; Review Warnings can open Review while project save state remains Saved and no geometry mutation occurs. | Iteration 030 milestone proof and dashboard fit evidence. |
| 030 | Curate third milestone evidence for dashboard, walkthrough, recent-project, and Review Warnings browser proof. | Iterations 021-029 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-030/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-030/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` d176df32; `SHA256SUMS.txt` 802803bf. | `c39e4b2` | Pass; milestone proof confirms 48 browser checks and curated dashboard hashes without checking in raw screenshots. | Continue dashboard/walkthrough hardening through iteration 035. |
| 031 | Make Review Warnings Inspect Map navigation open the map in read-only review mode and prove it does not dirty project geometry. | Dashboard warning actions from iteration 029; browser map Review Layout no-mutation status. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 25df28bb; `browser-workflow.spec.ts` e22e120b. | `f43063b` | Pass; Inspect Map switches to Review Layout, opens the map workbench, and keeps project save state Saved. | Recent-project saved-state persistence proof. |
| 032 | Prove reopening a saved browser project from Recent Projects restores the Saved state. | Web local project repository recent row behavior; dashboard save-state pill from iteration 026. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 99879a7d. | `fc101f0` | Pass; saved project reopen restores the dashboard project and leaves the save-state pill at Saved. | Walkthrough reset project-scope proof. |
| 033 | Prove Reset Walkthrough clears only the active project's browser-local walkthrough progress. | Project-scoped walkthrough localStorage key from iteration 027; dashboard reset control. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 24434349. | `4348bbb` | Pass; reset clears the active project while preserving Sample walkthrough progress in its project-scoped key. | Dashboard workflow next-step after walkthrough progress. |
| 034 | Prove dashboard next-step guidance advances from imagery attribution to boundary review after imagery walkthrough progress. | `recommendedWorkflowStep` dashboard logic and browser-local walkthrough progress. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` c459bf3a. | `4eade44` | Pass; imagery walkthrough progress advances the dashboard next step while leaving the project Saved. | Dashboard accessibility and specialist checkpoint. |
| 035 | Add explicit accessible labels for walkthrough reset and module controls, with proof through the reset workflow. | Interface specialist checkpoint cadence; walkthrough dashboard controls. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`; interface specialist checkpoint. | `App.tsx` 249028f9; `browser-workflow.spec.ts` 7f016f60. | `0207c45` | Pass; reset and module controls expose explicit names while the reset proof uses the active-project reset label. | Expert Review evidence labeling. |
| 036 | Prove walkthrough module controls by checkbox role, accessible name, checked state, and keyboard activation. | Interface specialist checkpoint finding; explicit walkthrough labels from iteration 035. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 600da4c0; `browser-workflow.spec.ts` 4623fee8. | `59de219` | Pass; web walkthrough checkboxes now expose aria-checked and toggle through Space/Enter while keeping progress local. | Offline imagery accepted workflow path. |
| 037 | Let offline imagery progress advance the dashboard next step after the imagery walkthrough checkpoint is complete. | Interface specialist checkpoint finding; `recommendedWorkflowStep` ordering and offline overlay workflow. | `apps/mobile/App.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `App.tsx` 169f7a1c; `browser-workflow.spec.ts` 04d71ab7. | `2ad7ddc` | Pass; imagery Off remains accepted when the imagery checkpoint is complete and the dashboard advances to boundary review without dirtying the project. | Expert Review evidence labeling. |
| 038 | Label Expert Review finding sections for evidence, acceptance gates, and actions, with browser proof. | Expert Review finding surface and evidence-labeling batch focus. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` 85494260; `browser-workflow.spec.ts` cd0e8125. | `d91064b` | Pass; Review findings now separate evidence, acceptance gates, and actions while the project remains Saved. | Expert Review recommendation preview safety. |
| 039 | Prove generated Expert Review recommendation previews do not dirty or apply projected geometry. | Expert Review candidate generation and preview state. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 2f5818cd. | `972248e` | Pass; generated pivot candidates can enter Previewing state while save state stays Saved and no projected geometry is applied. | Iteration 040 milestone proof and Expert Review evidence. |
| 040 | Run fourth milestone gates and curate browser/Expert Review evidence for iterations 031-039. | Iterations 031-039 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-040/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-040/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` fa8476f0; `SHA256SUMS.txt` 8acca7b0. | `29c8d02` | Pass; milestone proof confirms 69 browser checks and curated Expert Review/dashboard hashes without checking in raw screenshots. | Continue Expert Review warning quality and apply confirmation safety. |
| 041 | Make Expert Review Apply confirmation explicit about projected `XY` safety and prove it stays non-mutating before operator confirmation. | Expert Review apply confirmation flow and generated pivot recommendations. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` ae9256f1; `browser-workflow.spec.ts` 6727f85a. | `059da99` | Pass; Apply opens an explicit projected `XY` confirmation while save state stays Saved and Cancel leaves geometry unchanged. | Expert Review apply confirmation wording. |
| 042 | Give the Expert Review Apply confirmation a stable browser proof surface and alert semantics. | Expert Review apply confirmation accessibility and test target stability. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` 6f0ac11f; `browser-workflow.spec.ts` 982a50b4. | `ec84a1e` | Pass; Apply confirmation is targetable as `review-apply-confirmation`, exposes alert semantics, and still clears without dirtying the project. | Expert Review cancel status clarity. |
| 043 | Make canceling an Expert Review Apply confirmation report that projected `XY` geometry was unchanged. | Expert Review Apply cancel path and confirmation status line. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` 87659a5b; `browser-workflow.spec.ts` 1eb9d552. | `168bd69` | Pass; Cancel clears the Apply confirmation, reports that projected `XY` geometry was unchanged, and leaves the project Saved. | Expert Review manual decision safety. |
| 044 | Make manual Expert Review Accept decision feedback explicit that it does not mutate projected `XY` geometry. | Expert Review decision status messaging and generated recommendation decisions. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` fcb18ee8; `browser-workflow.spec.ts` a9a92aaa. | `af990ea` | Pass; Accept records review evidence, reports that projected `XY` geometry was unchanged, and keeps the project Saved. | Expert Review reject/defer safety. |
| 045 | Prove manual Expert Review Reject records a decision without mutating projected `XY` geometry. | Expert Review decision safety regression around generated recommendation rejection. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "expert review reject records"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 6497c89e. | `fdc7dde` | Pass; Reject records review evidence, reports no projected `XY` mutation, and keeps the project Saved across all target widths. | Expert Review defer safety. |
| 046 | Prove manual Expert Review Defer records a decision without mutating projected `XY` geometry. | Expert Review decision safety regression around generated recommendation deferral. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "expert review defer records"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 1634c897. | `693e8ec` | Pass; Defer records review evidence, reports no projected `XY` mutation, and keeps the project Saved across all target widths. | Expert Review recommendation action accessibility. |
| 047 | Scope repeated Expert Review recommendation action buttons with recommendation-aware accessible names. | Expert Review generated recommendation action accessibility and browser selectors. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` e022462b; `browser-workflow.spec.ts` 72aaf6fc. | `fab695d` | Pass; recommendation actions expose contextual accessible names while visible labels remain compact and browser proof passes at all target widths. | Expert Review applied geometry save-state proof. |
| 048 | Prove Expert Review Apply XY dirties the project only after the operator confirms. | Expert Review positive apply flow and project save-state behavior. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "expert review apply xy marks"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 0394bb99. | `1cdc59c` | Pass; Apply XY leaves the project Saved before confirmation, then reports applied projected geometry and shows Unsaved edits after confirmation. | Expert Review apply status recovery. |
| 049 | Clarify post-Apply XY status so operators know Save Local persists the edited project. | Expert Review applied-geometry status messaging and positive apply browser proof. | `apps/mobile/src/components/ExpertReviewPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run proof:web`; `git diff --check`; `npm audit`. | `ExpertReviewPanel.tsx` 0383128e; `browser-workflow.spec.ts` 5dc6d581. | `bda0cf9` | Pass; applied-geometry status now points operators to Save Local for persistence and full browser proof passes. | Iteration 050 Expert Review milestone proof. |
| 050 | Run fifth milestone gates and curate Expert Review apply/decision safety evidence for iterations 041-049. | Iterations 041-049 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-050/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-050/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` 42db5bad; `SHA256SUMS.txt` 2713a9b2. | `e989d70` | Pass; milestone proof confirms 84 browser checks and curated Expert Review apply/decision safety hashes without checking in raw screenshots. | Files/import/export readiness. |
| 051 | Give the Files panel status message a stable browser status surface and prove the canonical ZIP package guidance is visible. | Project Files panel status surface and archive-readiness messaging. | `apps/mobile/src/components/ProjectFilesPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "files status keeps"`; `git diff --check`; `npm audit`. | `ProjectFilesPanel.tsx` 880ff321; `browser-workflow.spec.ts` 7bd0bf7c. | `c54be07` | Pass; Files status has a stable web status surface and the canonical ZIP package guidance stays visible across target widths. | File action accessibility. |
| 052 | Mark Files panel actions as explicit browser buttons and prove archive/GIS import-export controls are reachable. | Project Files action roles and browser accessibility selectors. | `apps/mobile/src/components/ProjectFilesPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "files actions expose"`; `git diff --check`; `npm audit`. | `ProjectFilesPanel.tsx` ebbd5841; `browser-workflow.spec.ts` 1a4666b9. | `65c958b` | Pass; Files archive and GIS controls expose button roles and remain reachable across target widths. | ZIP export feedback. |
| 053 | Prove Export ZIP downloads the canonical `.center-pivot.zip` package and reports it in Files status. | Browser ZIP export feedback and canonical archive package flow. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "files export zip"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 15d18632. | `4a37583` | Pass; Export ZIP downloads a `.center-pivot.zip` package and the Files status reports the downloaded filename across target widths. | KML export feedback. |
| 054 | Prove Export KML downloads Google Earth visual-interchange data without making runtime render claims. | Browser KML export feedback and KML/KMZ styling boundary. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "files export kml"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` ebd15514. | `191ccec` | Pass; Export KML downloads `.google-earth.kml`, reports exported Google Earth feature count, and makes no render-proof claim. | KMZ export feedback. |
| 055 | Prove Export KMZ downloads a `doc.kml` archive without making runtime render claims. | Browser KMZ export feedback and KML/KMZ archive boundary. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "files export kmz"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` fd30dbe9. | `a53c182` | Pass; Export KMZ downloads `.google-earth.kmz`, reports `doc.kml` contents, and makes no render-proof claim. | Projected GeoJSON import readiness. |
| 056 | Prove projected GeoJSON import applies project-CRS field boundary data and dirties the project. | Projected GeoJSON import input and projected `XY` save-state feedback. | `apps/mobile/src/components/ProjectFilesPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "files projected geojson"`; `git diff --check`; `npm audit`. | `ProjectFilesPanel.tsx` 85af77e2; `browser-workflow.spec.ts` 6fb23daf. | `a366605` | Pass; projected GeoJSON field boundary import updates the current project, reports the import, and marks the project Unsaved edits. | GeoJSON WGS84 rejection feedback. |
| 057 | Prove GeoJSON labeled as WGS84 is rejected and cannot dirty canonical projected geometry. | GeoJSON import CRS guardrail and browser save-state proof. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "files geojson import rejects"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 89eb7cd7. | `a34212c` | Pass; WGS84 GeoJSON is rejected as input/display-only and the project remains Saved across target widths. | Survey CSV import readiness. |
| 058 | Prove projected Survey CSV import adds a point and dirties the project. | Survey CSV import input and projected point save-state feedback. | `apps/mobile/src/components/ProjectFilesPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "files survey csv import"`; `git diff --check`; `npm audit`. | `ProjectFilesPanel.tsx` c5a98f88; `browser-workflow.spec.ts` 576af9fd. | `84bd1ed` | Pass; projected Survey CSV import reports one survey point and marks the project Unsaved edits. | Survey CSV missing-XY rejection feedback. |
| 059 | Prove Survey CSV without projected `x,y` columns is rejected and cannot dirty the project. | Survey CSV import projected-coordinate guardrail and browser save-state proof. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "files survey csv import rejects"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 659bd74f. | `72f95f9` | Pass; CSV without projected `x,y` columns is rejected and the project remains Saved across target widths. | Iteration 060 files/import/export milestone proof. |
| 060 | Run sixth milestone gates and curate Files/import/export evidence for iterations 051-059. | Iterations 051-059 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-060/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-060/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` 9fd4aff7; `SHA256SUMS.txt` c7511d9d. | `95adc5c` | Pass; milestone proof confirms 111 browser checks and curated Files/export/import hashes without checking in raw screenshots. | Continue files/import/export readiness. |
| 061 | Prove projected GeoJSON import can be persisted with Save Local after dirtying the project. | GeoJSON import save-recovery workflow and browser save-state proof. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "projected geojson import can be saved"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 81849cdb. | `5ba9813` | Pass; projected GeoJSON import moves to Unsaved edits and Save Local returns the project to Saved across target widths. | Survey CSV save recovery. |
| 062 | Prove Survey CSV import can be persisted with Save Local after dirtying the project. | Survey CSV import save-recovery workflow and browser save-state proof. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey csv import can be saved"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` b2e83e46. | `b27675a` | Pass; Survey CSV import moves to Unsaved edits and Save Local returns the project to Saved across target widths. | Import input reset proof. |
| 063 | Prove successful projected GeoJSON import clears the paste field after applying data. | GeoJSON import cleanup feedback and stale-input prevention. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "projected geojson import clears"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 036a7696. | `88e71b2` | Pass; successful projected GeoJSON import clears the paste field across target widths. | CSV import input reset proof. |
| 064 | Prove successful Survey CSV import clears the paste field after applying data. | Survey CSV import cleanup feedback and stale-input prevention. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey csv import clears"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 8d82c67b. | `a0853b8` | Pass; successful Survey CSV import clears the paste field across target widths. | Rejected import input preservation. |
| 065 | Prove rejected GeoJSON import preserves the paste field for operator correction. | GeoJSON import error recovery and no-mutation browser proof. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "rejected geojson import preserves"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` b4db1181. | `ae55fec` | Pass; rejected WGS84 GeoJSON remains editable in the paste field and the project stays Saved across target widths. | Settings/offline imagery guardrails. |
| 066 | Add stable Settings imagery guardrail anchors and prove imagery Off reports the local-only export boundary. | Settings/offline imagery guardrail surface and browser network proof. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "settings offline imagery guardrail"`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` f1b5de0c; `browser-workflow.spec.ts` b7f45ba4. | `7018094` | Pass; imagery Off summary is targetable and reports no external tile source plus projected/local `XY` export boundary. | Settings tile-cap control proof. |
| 067 | Add accessible Settings stepper controls and prove the imagery tile cap clamps the interactive preview budget. | Settings tile-cap control and no-cost imagery preview limits. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "settings tile cap stepper"`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` f05451a4; `browser-workflow.spec.ts` 5d85330c. | `ced1da0` | Pass; tile-cap controls expose named buttons and clamp the browser preview budget from 8 to 128 tiles without dirtying the project. | Custom imagery validation proof. |
| 068 | Prove custom open imagery rejects credentialed tile templates before they can alter browser imagery settings. | Core custom imagery safety validation and Settings custom source form. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings custom imagery rejects"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 871cb46f. | `2e88583` | Pass; credentialed tile templates keep Apply disabled, live imagery disabled, and project state Saved. | Custom open source apply proof. |
| 069 | Prove a no-key local custom imagery template can be applied as a browser-local preview source. | Custom imagery validation allowlist for local self-hosted tile services and Settings apply flow. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings custom imagery applies"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 93301bab. | `6e1ed52` | Pass; valid local templates enable Custom open preview guidance while keeping imagery reference-only and project state Saved. | Iteration 070 settings milestone proof. |
| 070 | Run seventh milestone gates and curate Files recovery plus Settings imagery guardrail evidence. | Iterations 061-069 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-070/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-070/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` 2665d833; `SHA256SUMS.txt` 80d17c3e. | `6a8aaf3` | Pass; milestone proof confirms 138 browser checks and curated Settings/offline imagery hashes without checking in raw screenshots. | Survey/RTK browser workflow integration. |
| 071 | Add a stable Settings offline-package guardrail anchor and prove network tiles remain disabled. | Offline package Settings summary and offline-first map package boundaries. | `apps/mobile/src/components/SettingsPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "settings offline package guardrail"`; `git diff --check`; `npm audit`. | `SettingsPanel.tsx` baa9a0c5; `browser-workflow.spec.ts` 5bba0979. | `5e3c387` | Pass; offline package summary exposes disabled network tiles, required attribution, local directory, and package-type controls while project state stays Saved. | Offline package type selection proof. |
| 072 | Prove offline package type changes preserve local-only package guardrails. | Offline package preferred-type settings and browser-local project state. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings offline package type"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 50117171. | `45af17a` | Pass; MBTiles and raster package choices keep network tiles disabled, attribution required, local directory visible, and project state Saved. | Settings map-style/offline imagery separation. |
| 073 | Prove Settings map-style changes do not enable online imagery or alter projected map geometry status. | Map style Settings controls and online imagery Off guardrail. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings map style changes"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 62a97818. | `8c39513` | Pass; selecting the Imagery map style keeps online imagery Off, map status on offline overlay, and project state Saved. | Offline imagery map request proof. |
| 074 | Prove imagery Off blocks map tile requests after a live source is active. | Settings online imagery Off transition and strict offline browser request capture. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings offline imagery off blocks"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 7cfa2749. | `482e05d` | Pass; after live imagery is visible then Off is selected, opening the map shows no live imagery and records no external tile requests. | Settings export-exclusion proof. |
| 075 | Prove browser-local imagery and package settings stay out of exported Project ZIP data. | Project archive export boundary and Settings local-only imagery/package controls. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "settings browser-local imagery"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` f33dd14a. | `f32fc2e` | Pass; exported `project.json` excludes `onlineImagery`, tile URL templates, local custom source names, and local package directories after Settings changes. | Survey/RTK browser workflow integration. |
| 076 | Add stable RTK gate/status anchors and prove the browser receiver starts closed without mutating the project. | Browser RTK receiver panel and configured RTK quality gate. | `apps/mobile/src/components/BrowserRtkReceiverPanel.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "survey rtk receiver starts"`; `git diff --check`; `npm audit`. | `BrowserRtkReceiverPanel.tsx` 62f894f6; `browser-workflow.spec.ts` e9aa00f4. | `d4a4acf` | Pass; RTK gate/status surfaces show Gate closed, unknown fix reasons, disabled capture, and project state Saved. | RTK closed capture button proof. |
| 077 | Prove a closed RTK gate disables survey geometry capture controls. | Browser RTK receiver geometry controls and quality gate state. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey rtk closed gate"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 12a782b7. | `15514ac` | Pass; closed RTK gate disables boundary, obstacle, and feature capture controls while project state remains Saved. | RTK role selection no-mutation proof. |
| 078 | Prove RTK survey role selection stays local while the quality gate is closed. | Browser RTK survey role controls and project dirty-state boundary. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey rtk role selection"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` b1eb2f61. | `8f41402` | Pass; Pivot/Water/Power role choices leave capture disabled, gate closed, and project state Saved. | RTK map-feature option no-mutation proof. |
| 079 | Prove RTK map-feature option changes stay local while the quality gate is closed. | Browser RTK map-feature controls and project dirty-state boundary. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey rtk map feature selection"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 42b39dcc. | `82de03c` | Pass; point and line feature options leave save controls disabled, gate closed, and project state Saved. | Iteration 080 Survey/RTK milestone proof. |
| 080 | Run eighth milestone gates and curate Settings/Survey RTK evidence for iterations 071-079. | Iterations 071-079 commits; Playwright proof outputs under ignored `reports/continuous-improvement/`; CPLayout milestone cadence. | `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-080/README.md`; `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-080/SHA256SUMS.txt`; loop ledger. | `npm run validate`; `npm run validate:skills`; `npm run audit:moderate`; `npm audit`; `git diff --check`; `npm run proof:web`. | `README.md` 85b74555; `SHA256SUMS.txt` 877d56f7. | `d02a3bf` | Pass; milestone proof confirms 165 browser checks and curated Settings/Survey RTK hashes without checking in raw screenshots. | Continue Survey/RTK browser workflow integration. |
| 081 | Prove imported projected Survey CSV evidence appears in the Survey readiness workflow. | Files CSV import to Survey view handoff and browser-visible survey metrics/list anchors. | `apps/mobile/App.tsx`; `apps/mobile/src/components/MetricTile.tsx`; `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run export:web`; `npm run test:web:e2e -- --grep "survey view reflects imported projected survey csv evidence"`; `git diff --check`; `npm audit`. | `App.tsx` 918b2675; `MetricTile.tsx` 93a71e17; `browser-workflow.spec.ts` 36eddd3b. | `338dced` | Pass; imported projected CSV evidence updates Survey metrics/list state, preserves the Unsaved edits boundary, and proves the handoff across all target widths. | Survey point promotion proof. |
| 082 | Prove Survey point promotion writes projected water-source geometry only after explicit operator action. | Survey point promotion, canonical ZIP export inspection, and projected `XY` infrastructure boundary. | `tests/web/browser-workflow.spec.ts`; loop ledger. | `npm run validate`; `npm run test:web:e2e -- --grep "survey point promotion writes projected water source"`; `git diff --check`; `npm audit`. | `browser-workflow.spec.ts` 54431e3c. | Pending until committed. | Pass; imported RTK-fixed water-source evidence stays survey evidence until `Set Water`, then exported `project.json` contains the promoted projected `waterSource`. | Survey point delete proof. |

## Milestones

### Iteration 010

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-010/README.md`.

Summary: the first milestone curates browser-only evidence for static web export, Playwright route screenshots/checks, Review Layout no-mutation behavior, draft-clearing behavior, and HUD no-overlap checks. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 020

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-020/README.md`.

Summary: the second milestone curates browser-only evidence for feature selection, strict offline map operation, Settings imagery guardrails, custom no-key source guidance, and explicit projected `XY` status for boundary, Utility line, and Utility point saves. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 030

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-030/README.md`.

Summary: the third milestone curates browser-only evidence for dashboard next-step priority, walkthrough project scoping, recent-project start/reopen states, and Review Warnings navigation into read-only Review. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 040

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-040/README.md`.

Summary: the fourth milestone curates browser-only evidence for Review Warnings map inspection, recent-project saved-state recovery, walkthrough accessibility and offline-imagery progress, and Expert Review evidence labeling plus non-mutating preview behavior. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 050

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-050/README.md`.

Summary: the fifth milestone curates browser-only evidence for Expert Review Apply confirmation, manual decision safety, recommendation action accessibility, and the Apply XY dirty-state boundary. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 060

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-060/README.md`.

Summary: the sixth milestone curates browser-only evidence for Files status, archive export, KML/KMZ visual interchange downloads, projected GeoJSON import/rejection, and Survey CSV import/rejection. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, or any imagery/CV promotion into canonical geometry.

### Iteration 070

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-070/README.md`.

Summary: the seventh milestone curates browser-only evidence for Files import recovery plus Settings imagery guardrails, including imagery Off local-only status, tile-cap controls, credential rejection, and valid no-key local custom source application. It does not verify native runtime behavior, raw tile-package rendering, Google Earth rendering, external imagery availability, or any imagery/CV promotion into canonical geometry.

### Iteration 080

Checked-in evidence: `docs/evidence/continuous-improvement/browser-mapping-2026-05-31/iteration-080/README.md`.

Summary: the eighth milestone curates browser-only evidence for Settings offline package guardrails, map-style/offline imagery separation, browser-local export exclusion, and Survey/RTK closed-gate controls. It does not verify native runtime behavior, raw tile-package rendering, live GNSS/RTK receiver integration, Google Earth rendering, or any imagery/CV promotion into canonical geometry.
